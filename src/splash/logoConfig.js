/**
 * MUSGO wordmark tweaks.
 *
 * LOGO_VIEWPORT_SCALE — fraction of min(vw, vh); GLOW used 0.8.
 * WHITE_OFFSET_X — horizontal shift of the white SVG layer (CSS px).
 */

/** Overall logo size vs viewport (was 0.8). */
export const LOGO_VIEWPORT_SCALE = 0.55;

/** White SVG layer displacement on X (positive = right). */
export let WHITE_OFFSET_X = -8;
